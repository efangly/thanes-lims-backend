# Thanes LIMS Backend — งานที่เหลือ (Phase ถัดไป)

## Redis Cache — Refresh Token + Location Full Path (เริ่ม 2026-08-26)

ระบบยังไม่มี caching layer เลย ตัดสินใจผ่าน grilling session แล้วว่าจะเพิ่ม Redis เป็น read-through cache สำหรับสองจุด: (1) Refresh Token revocation lookup (fast-path, fail-closed) และ (2) `Location.FullPath` (TTL-only, fail-open) — ดูรายละเอียดการตัดสินใจใน [`CONTEXT.md`](./CONTEXT.md#caching) และ [`docs/adr/0005-redis-cache-for-refresh-tokens-and-location-full-path.md`](./docs/adr/0005-redis-cache-for-refresh-tokens-and-location-full-path.md) — instance จริงมีอยู่แล้วที่ `REDIS_URL` ใน `.env` (shared กับ MinIO deployment) ยังไม่มีโค้ดต่อเข้าไปใช้

Access token blacklist และ RBAC permission caching อยู่นอกขอบเขต (ตัดสินใจไว้ชัดเจนใน ADR 0005 — ADR 0002 จงใจไม่ cache สองจุดนี้อยู่แล้ว)

- [x] **Phase 1 — Cache port/adapter foundation**: เพิ่ม `github.com/redis/go-redis/v9` เป็น dependency, `internal/ports/cache/cache.go` (interface `Cache` — `Get`/`Set`/`Delete`, generic ใช้ร่วมกันทั้งสอง use case, `Get` คืน `shared.ErrNotFound` ตอน miss ตาม convention เดิม), `internal/adapters/redis/client.go` (concrete implementation ผูก go-redis จริง, `New` parse `REDIS_URL` + PING ตอน boot), เพิ่ม `RedisURL string \`env:"REDIS_URL,required"\`` ใน `internal/config/config.go` + `.env.example`, wire เข้า `cmd/api/main.go` (fail-fast ตอน boot ถ้า connect ไม่ได้ เหมือน Postgres/MinIO, `defer redisCache.Close()`) — `go build`/`go vet`/`go test ./...` ผ่านหมด (ยังไม่ได้ใช้ cache จริงในรอบนี้ ตาม scope ที่ตกลงไว้)
- [x] **Phase 2 — Refresh Token fast-path cache (fail-closed)**: implement เป็น decorator `internal/adapters/cacheduser.CachedRefreshTokenRepository` (ครอบ `portuser.RefreshTokenRepository` เดิม + `cache.Cache`) แทนการแก้ use case ตรง ๆ — `login.go`/`refresh.go`/`logout.go`/`logout_all.go`/`update_user.go` ไม่ต้องแก้เลย เพราะพึ่ง interface เดิม, wiring เดียวที่เปลี่ยนคือ `cmd/api/routes.go` (`refreshRepo := cacheduser.NewCachedRefreshTokenRepository(postgresuser.NewRefreshTokenRepository(gdb), redisCache)`)
  - แคช full row คีย์ `refresh:{token_hash}`, TTL = เวลาที่เหลือถึง `ExpiresAt` — populate ตอน `Create` (Login/Rolling Refresh) และตอน cache-miss fallback
  - `FindByTokenHash`: cache hit → คืนเลย (skip Postgres); cache miss (`shared.ErrNotFound`) → fallback Postgres แล้ว populate; **cache error อื่น (Redis unreachable) → fail-closed คืน error ทันที ไม่ fallback Postgres** ตามที่ตกลงไว้
  - แก้ signature `RefreshTokenRepository.Revoke` เป็น `Revoke(ctx, id, tokenHash)` (เพิ่ม `tokenHash` เพื่อให้ decorator invalidate cache ได้โดยไม่ต้อง query เพิ่ม — caller มี `stored.TokenHash` อยู่แล้วจาก `FindByTokenHash` ก่อนหน้าเสมอ) และเพิ่ม `FindTokenHashesByUserID` เข้า interface + Postgres implementation (`Pluck` เอาเฉพาะ token_hash ที่ยัง `revoked = false`)
  - `RevokeAllForUser` (decorator): query token_hash ทั้งหมดของ user จาก Postgres ก่อน → revoke ใน Postgres → loop `Delete` ใน Redis ทีละ key (ตาม Q9=a ไม่เก็บ index แยกใน Redis) — cache-delete error ต่อ key แค่ log ไม่ fail ทั้ง call (Postgres revoke สำเร็จแล้วเป็นหลัก)
  - `Revoke` เดี่ยว: cache-delete error **propagate** ขึ้นไป (ต่างจาก revoke-all) เพราะเป็น single-token invalidation ที่ยืนยันได้ตรง ๆ
  - Unit test มี fake mock repo/cache ครบทุก branch (`internal/adapters/cacheduser/refresh_token_repository_test.go`) — ผ่านหมด
  - Integration test จริงกับ Postgres+Redis container คู่กัน (`internal/adapters/cacheduser/refresh_token_repository_integration_test.go` + `internal/adapters/redis/client_integration_test.go`, ใช้ helper ใหม่ `internal/adapters/redis/rtest/container.go` เลียนแบบ `pgtest`) — compile ผ่านด้วย `go build/vet -tags=integration ./...` แต่ **รันจริงไม่ได้ในเครื่องนี้เพราะไม่มี Docker daemon** (เหมือนข้อจำกัดเดิมของ location integration test) ต้องรันบนเครื่อง/CI ที่มี Docker เพื่อ verify จริงอีกที
  - `go build`/`go vet`/`go test ./...` ผ่านหมด (รวม mocks เดิมใน `internal/application/user/mocks_test.go`/`refresh_test.go` ที่ต้องอัปเดตตาม signature ใหม่)
- [x] **Phase 3 — Location Full Path cache (fail-open, TTL 15m)**: decorator pattern เดียวกับ Phase 2 — `internal/adapters/cachedlocation.CachedRepository` ครอบ `portlocation.LocationRepository` เดิม, cache เฉพาะ `FullPath` เท่านั้น method อื่นทั้งหมด (`Create`/`CreateMany`/`GetByID`/`ListChildren`/`FindChildByName`/`HasChildren`/`Delete`) passthrough ตรง ๆ — use case ทั้งหมด (`get_full_path.go`, `generate_report.go`, `assign_location.go`, ฯลฯ) ไม่ต้องแก้เลย, wiring เดียวที่เปลี่ยนคือ `cmd/api/routes.go` (`locationRepo := cachedlocation.NewCachedRepository(postgreslocation.New(gdb), redisCache)`)
  - คีย์ `location:fullpath:{id}`, TTL คงที่ 15 นาที (`fullPathTTL` = `JWT_ACCESS_TTL`)
  - **Fail-open**: cache hit → คืนเลย; cache miss **หรือ error ใดๆ (รวม Redis unreachable)** → fallback query Postgres เหมือนเดิมทุกกรณี แล้ว best-effort populate cache (error ตอน populate แค่ log ไม่ throw) — ต่างจาก Refresh Token cache (Phase 2) ที่ fail-closed
  - ไม่มี invalidation แบบ event-driven ตามที่ตกลงไว้ (Q10=a) — ปล่อยให้ TTL หมดอายุเองถ้า location ถูกแก้/ย้าย
  - Unit test ครบทุก branch (`internal/adapters/cachedlocation/repository_test.go`) — ผ่านหมด
  - Integration test จริงกับ Postgres+Redis (`internal/adapters/cachedlocation/repository_integration_test.go`) พิสูจน์ทั้ง cache population และ TTL-only staleness (rename ตรงใน Postgres แล้วยืนยันว่า cache ยังคืนชื่อเก่าจนกว่า TTL หมด) — compile ผ่านด้วย `go build/vet -tags=integration ./...` แต่รันจริงไม่ได้ในเครื่องนี้เพราะไม่มี Docker daemon (เหมือน Phase 2)
  - `go build`/`go vet`/`go test ./...` ผ่านหมด
- [x] **Phase 4 — Docs/cleanup**: `.env.example` มี `REDIS_URL` แล้ว (เพิ่มไว้ตั้งแต่ Phase 1), ไม่มี endpoint ใหม่เกิดขึ้นเลยทั้ง 3 phase (เป็น decorator ล้วน ไม่แตะ HTTP layer) จึงไม่ต้องรัน `make swagger`, `go mod tidy` ผ่านไม่มีอะไรเปลี่ยน, `go build`/`go vet`/`go test ./...` ทั้งแบบปกติและ `-tags=integration` ผ่านหมด (integration test รันจริงไม่ได้ในเครื่องนี้เพราะไม่มี Docker daemon — ต้อง verify บนเครื่อง/CI ที่มี Docker)

**สถานะรวม: ครบทั้ง 4 phase แล้ว — เพิ่ม Redis เป็น read-through cache สำเร็จผ่าน decorator pattern 2 ตัว (`cacheduser.CachedRefreshTokenRepository` fail-closed, `cachedlocation.CachedRepository` fail-open) โดยไม่แตะ use case/HTTP layer เดิมเลย นอกจาก signature เล็กน้อยของ `RefreshTokenRepository.Revoke`/`FindTokenHashesByUserID`. ยังไม่ได้ verify integration test จริงกับ Docker (ไม่มีในเครื่องนี้), ยังไม่ได้ `git commit`**

# Thanes LIMS Backend — งานที่เหลือ (Phase ถัดไป)

Phase 1 (Auth/Sample/TestResult) และ Phase 2 (Equipment/Inventory+PO/Documents/Environment/Notification) เสร็จสมบูรณ์แล้ว พร้อม unit test และ verify จริงกับ Postgres+MinIO ผ่านหมด รายการด้านล่างคือสิ่งที่ถูก defer ไว้ตอน grilling session ตั้งแต่ต้น หรือพบว่าควรทำเพิ่มระหว่างพัฒนา

## Real-time / Integration

- [x] Environment module: เพิ่ม WebSocket broadcast layer สำหรับ alert แบบ real-time (ตอนนี้เป็น REST เท่านั้นตามที่ตกลงไว้ใน Phase 2) — ใช้ `contrib/websocket` ของ Fiber v3 (หมายเหตุ: `gofiber/contrib/websocket` ยังไม่รองรับ Fiber v3 จริง — ใช้ `github.com/fasthttp/websocket` ตรงกับ `c.RequestCtx()` แทน ดู `internal/adapters/http/environment/ws_hub.go` + route `GET /environment/alerts/ws` (auth ผ่าน query param `?token=`))
- [x] เชื่อม `Notifier` port (มี `AsNotifier` adapter พร้อมใช้ใน `internal/application/notification/create_notification.go` แล้ว) เข้ากับ use case ของ module อื่นที่ควร trigger notification จริง:
  - [x] Environment: สร้าง alert ใหม่ (crit/warn) → แจ้งเตือน
  - [x] Inventory: item ต่ำกว่า min → แจ้งเตือน (ตอนนี้ reorder ต้องกดเองผ่าน `POST /inventory/:id/reorder`)
  - [x] TestResult: approve เสร็จ → แจ้งเตือนผู้เกี่ยวข้อง

## Reporting

- [x] แทนที่ stub endpoint `GET /tests/:id/report` และ `GET /audit/export` (ตอนนี้คืน 501) ด้วย PDF generation จริงด้วย `gofpdf` เมื่อ requirement เรื่อง format ชัดเจน (หมายเหตุ: format ยังไม่มีสเปกจากธุรกิจ จึงออกแบบ layout เองแบบเรียบง่าย — เพิ่ม field/ปรับ branding ได้ภายหลังถ้ามี requirement ชัดเจนกว่านี้)
  - `GET /tests/:id/report`: PDF ผลการทดสอบ + ข้อมูลตัวอย่าง + chain-of-custody trail (`internal/application/testresult/generate_report.go`, `internal/adapters/pdf/testresult_report.go`)
  - `GET /audit/export`: PDF ตาราง audit log พร้อม filter ช่วงเวลา `?from=&to=` (RFC3339 หรือ `YYYY-MM-DD`), จำกัดสิทธิ์เฉพาะ admin/qa (`internal/adapters/http/audit/`, เพิ่ม `AuditLogger.List` ใน `internal/ports/audit/audit.go` ที่แต่ก่อนมีแค่ `Log`)
  - ใช้ `github.com/jung-kurt/gofpdf` + ฟอนต์ Sarabun (SIL OFL, embed ผ่าน `go:embed` ใน `internal/adapters/pdf/fonts/`) เพราะฟอนต์ core ของ PDF ไม่รองรับภาษาไทย

## Testing

- [x] เพิ่ม integration test ด้วย `testcontainers-go` เริ่มจาก `user`/`sample` repository (หมายเหตุ: dependency ยังไม่ได้ติดตั้งจริงตอนเริ่มงานนี้ ต้อง `go get` เพิ่มเอง — เพิ่ม `testcontainers-go` + `.../modules/postgres` และ `golang-migrate/migrate/v4` เป็น Go library สำหรับรัน migration ใส่ container) รันแยกจาก unit test ด้วย `go test -tags=integration ./...` หรือ `make test-integration` (ดู `internal/adapters/postgres/pgtest/container.go` สำหรับ helper spin-up container + apply migrations, `internal/adapters/postgres/user/repository_integration_test.go`, `internal/adapters/postgres/sample/repository_integration_test.go`) — ยังไม่ได้ขยายไปโมดูลอื่น (equipment/inventory/purchaseorder/document/environment/notification/testresult/audit) เหลือเป็นงานต่อไป
- [x] ตั้ง CI pipeline (GitHub Actions หรืออื่นๆ) รัน `go build`, `go vet`, `go test` อัตโนมัติทุก PR (`.github/workflows/ci.yml` — รันบน push/PR เข้า `main`; ไม่รวม `test-integration` เพราะต้องใช้ Docker daemon รัน testcontainers)

## API Documentation

- [x] เพิ่ม Swagger/OpenAPI docs ผ่าน `swaggo/swag` + `gofiber/swagger` (ถูก defer ไว้ตั้งแต่ grilling session) — ใช้ `github.com/gofiber/contrib/v3/swaggo` แทน `gofiber/swagger` เพราะตัวหลัง deprecated แล้วและ maintainer ย้ายไป contrib/v3 (ดู `internal/adapters/http/*/handler.go` สำหรับ `@Summary`/`@Router` annotations ของทุก endpoint, `cmd/api/main.go` สำหรับ general API info + `BearerAuth` security scheme, `cmd/api/routes.go` สำหรับ route `GET /api/v1/swagger/*`) รัน `make swagger` เพื่อ regenerate `docs/` หลังแก้ annotation (ต้อง commit `docs/` เพราะ `cmd/api` import ตรงๆ)

## Equipment

- [x] พิจารณาเพิ่ม `calibration_events` history table แยกจาก `equipment` เพื่อเก็บ audit trail การสอบเทียบแต่ละครั้ง (ตอนนี้เก็บแค่ `last_calibrated_at`/`next_calibration_due` ล่าสุดบน row เดียว) — เพิ่ม migration `000014_create_calibration_events_table` (`equipment_id` FK, `calibrated_at`, `next_calibration_due`, `performed_by`, `notes`), domain `CalibrationEvent` + port `CalibrationRepository` (append-only, ตาม pattern ของ `coc_steps`/`doc_history`), `RecordCalibrationUseCase` แก้ให้ append event ทุกครั้งที่บันทึกการสอบเทียบ, เพิ่ม `ListCalibrationEventsUseCase` + endpoint `GET /equipment/:id/calibration-events` (ยังไม่มี integration test สำหรับ equipment module — เหลือเป็นงานต่อไปตามหัวข้อ Testing ด้านบน)

## Inventory / PurchaseOrder

- [x] พิจารณาทำ automated reorder (scheduled job ตรวจ item ต่ำกว่า min แล้วสร้าง PO อัตโนมัติ) แทนการกดเองผ่าน endpoint — ตอนนี้เป็น manual ตามที่ตกลงไว้สำหรับ MVP (เพิ่ม `default_vendor` (nullable) ใน `inventory_items` ผ่าน migration `000015`, ตั้งค่าได้ตอนสร้าง item หรือผ่าน `PATCH /inventory/:id/default-vendor`; เพิ่ม `AutoReorderJob` (`internal/application/purchaseorder/auto_reorder_job.go`) reuse `CreateFromLowStockUseCase` เดิม — สแกนทุก item ทุก tick, ข้าม item ที่ไม่มี default vendor หรือมี PO ค้างอยู่แล้ว (กันสร้างซ้ำ) — รันเป็น `time.Ticker` goroutine ใน `cmd/api/main.go` คุมด้วย env `AUTO_REORDER_ENABLED` (default true) / `AUTO_REORDER_INTERVAL` (default `1h`), หยุดสะอาดตอน graceful shutdown)

## RBAC

- [x] ทบทวนว่า permission ต้องแยกระดับ module หรือไม่ (ตอนนี้เป็น global-per-role ตามที่ตกลงไว้ — ถ้า requirement เปลี่ยนต้องปรับ `internal/domain/user/role.go`) — **ทบทวนแล้ว (2026-08-13): คงเป็น global-per-role ต่อไป** เหมาะกับ MVP ที่มี role น้อยและยังไม่มี requirement ต้องการความละเอียดระดับ module ถ้าอนาคตมี requirement ชัดเจนค่อยกลับมาปรับ

## Storage Location (ตู้/ชั้น/ช่อง/sub-ช่อง)

แทนที่ `Sample.Location` (text ธรรมดา) ด้วยตาราง `locations` แบบ self-referencing tree ตามที่ตกลงไว้ใน grilling session — ดูรายละเอียดการตัดสินใจใน [`CONTEXT.md`](./CONTEXT.md#storage-location) และ [`docs/adr/0001-self-referencing-tree-for-storage-location.md`](./docs/adr/0001-self-referencing-tree-for-storage-location.md)

- [x] Domain: สร้าง `internal/domain/location` — `Location` entity, `LevelType` enum (`cabinet`/`shelf`/`slot`/`sub_slot`) พร้อม validate ลำดับชั้น (ห้ามข้ามระดับ, อนุญาต leaf กลางทาง) (`internal/domain/location/location.go` — `LevelType.CanBeChildOf`, `Location.IsRoot`, `ValidateChild`; หมายเหตุ: "leaf" คำนวณจากการไม่มีลูกใน DB ไม่ใช่ field บน entity เอง จึงยังไม่มี `IsLeaf()` บน struct — จะอยู่ที่ repository/use case แทนตอน implement query)
- [x] Domain: แก้ `internal/domain/sample/sample.go` — เปลี่ยน `Location string` เป็น `LocationID *string` (nullable) — ตามด้วยแก้ทุกจุดที่อ้างอิง field เดิมให้ compile ผ่าน (postgres model/repo, http dto/handler, application `create_sample.go`, PDF report, Oracle POC mirror adapter + smoke test, seed script, unit/integration test) — `go build ./...`, `go vet ./...`, `go test ./...` ผ่านทั้งหมด
- [x] Migration `000016_create_locations_table` — ตาราง `locations` (`id` VARCHAR, `parent_id` FK self-ref, `name`, `level_type` VARCHAR + CHECK constraint ตาม convention เดิมของโปรเจกต์ (ไม่ใช้ native Postgres ENUM), timestamps), unique `(parent_id, name)`, partial unique index ชื่อ root (`WHERE parent_id IS NULL`) — รัน `make migrate-up` กับ remote dev DB ผ่านแล้ว
- [x] Migration `000017_add_location_id_to_samples` — เพิ่ม `location_id` FK nullable บน `samples`, ลบ column `location` เดิม, partial unique index กัน sample ซ้อนทับ leaf เดียวกัน (เฉพาะ status `pending`/`testing`/`completed`) — รัน `make migrate-up` กับ remote dev DB ผ่านแล้ว (หมายเหตุ: sample ที่ seed ไว้ก่อนหน้าตอนนี้มี `location_id` เป็น NULL ทั้งหมด เพราะไม่ migrate ข้อมูลเก่าตามที่ตกลงไว้ — ต้องรอ task "แก้ seed script" ด้านล่างเพื่อสร้างข้อมูลใหม่)
- [x] Ports: `internal/ports/location/location.go` — `LocationRepository` (Create, CreateMany, GetByID, ListChildren, FindChildByName, HasChildren, Delete, FullPath) — restrict-delete และ leaf/uniqueness ตรวจที่ application layer ผ่าน pre-check query (ตาม pattern เดิมของโปรเจกต์ใน `create_user.go` ไม่ parse DB error code) แทนที่จะพึ่ง FK constraint error โดยตรง; เพิ่ม `UpdateLocation`/`ExistsActiveByLocation`/`ExistsByLocation` ใน `internal/ports/sample/repository.go` ด้วย
- [x] ID generation: เพิ่ม sequence key `"location"` ให้ idgen สร้าง ID แบบ `LOC-00001` (`internal/application/location/id.go`, ใช้ตาราง `id_sequences` เดิมที่รองรับ scope ใหม่ได้โดยไม่ต้อง migrate)
- [x] Adapter: `internal/adapters/postgres/location/` — `model.go` + `repository.go` (CRUD, recursive CTE สำหรับ full path, bulk insert สำหรับ generate children); เพิ่ม `UpdateLocation`/`ExistsActiveByLocation`/`ExistsByLocation` ใน `internal/adapters/postgres/sample/repository.go`
- [x] Application: `internal/application/location/` — `CreateCabinetUseCase`, `GenerateChildrenUseCase` (prefix + count), `ListChildrenUseCase`, `GetFullPathUseCase`, `DeleteLocationUseCase` (restrict ถ้ามีลูกหรือมี sample อ้างอิงอยู่)
- [x] Application: `internal/application/sample/assign_location.go` — `AssignLocationUseCase` ผูก/ย้าย sample เข้า leaf location (validate leaf-only ผ่าน `HasChildren` + unique-among-active-status ผ่าน `ExistsActiveByLocation`) — `go build`/`go vet`/`go test ./...` ผ่านทั้งหมด (อัปเดต mock `SampleRepository` ใน `internal/application/{sample,testresult}/mocks_test.go` ให้ตรง interface ใหม่ด้วย)
- [x] Adapter: `internal/adapters/http/location/` — `handler.go` + `dto.go` + `routes.go`: `POST /locations` (สร้างตู้), `GET /locations?parent_id=` (list children, ไม่ระบุ = root), `POST /locations/:id/children` (generate children), `GET /locations/:id/full-path`, `DELETE /locations/:id` — ผูกเข้า `cmd/api/routes.go` แล้ว
- [x] Adapter: `internal/adapters/http/sample/dto.go`/`handler.go` — `CreateSampleRequest`/`SampleResponse` ใช้ `location_id` แทน `location` text แล้ว (ทำใน phase ก่อนหน้า); เพิ่ม endpoint ใหม่ `PATCH /samples/:id/location` (`AssignLocationRequest` → `AssignLocationUseCase`) สำหรับ put-away แยกจากตอนสร้าง sample
- [x] Seed: แก้ `cmd/seed/main.go` — เพิ่ม `seedLocations()` สร้าง 5 ตู้ (Fridge-A/Freezer-1/Shelf-B/Shelf-C/Shelf-D) × 3 ชั้น × 5 ช่อง = 75 leaf slots ผ่าน use case เดียวกับที่ API ใช้จริง (`CreateCabinetUseCase`/`GenerateChildrenUseCase`), แก้ `seedSamples()` ให้เรียก `AssignLocationUseCase` ผูกแต่ละ sample เข้า leaf ของตัวเอง (40 samples < 75 leaves จึงไม่มี sample ไหนแย่ง leaf กัน), เพิ่ม `"locations"` ใน `wipe()` table list, ลบ field `location` (freeform text) ออกจาก `sampleTypeSpecs` เพราะไม่ใช้แล้ว — รัน `go run ./cmd/seed -force` กับ remote dev DB ผ่านจริง: "created 5 cabinets, 75 leaf slots" และ "created 40 samples" สำเร็จไม่มี error
- [x] Docs: รัน `make swagger` regenerate `docs/` ให้ endpoint ใหม่ของ location + `PATCH /samples/:id/location` ขึ้น Swagger (ต้อง `go install github.com/swaggo/swag/cmd/swag@latest` ก่อนเพราะเครื่องยังไม่มี `swag` CLI) — `go build ./...` ผ่านหลัง regenerate
- [x] Test: unit test domain rules — `internal/domain/location/location_test.go` (`LevelType.Valid`/`Next`/`CanBeChildOf`, `ValidateChild`, `Location.IsRoot`) และ `internal/application/sample/assign_location_test.go` (mock-based: success, reject non-leaf, reject already-occupied leaf) — เพิ่ม `mockLocationRepo` ใน `internal/application/sample/mocks_test.go` — `go test ./internal/domain/location/... ./internal/application/sample/...` ผ่านหมด
- [x] Test: integration test `internal/adapters/postgres/location/repository_integration_test.go` (`//go:build integration`, ผ่าน `pgtest` container) — tree CRUD (create/CreateMany/GetByID/ListChildren/FindChildByName/HasChildren), full path, restrict-delete-with-children, root name uniqueness, not-found cases — คอมไพล์ผ่านด้วย `go build -tags=integration ./...` และ `go vet -tags=integration ./...` แต่ **รันจริงไม่ได้ในเครื่องนี้เพราะไม่มี Docker daemon** (เหมือนที่ CI ข้าม `test-integration` ไว้ตามหมายเหตุใน section Testing ด้านบน) — ต้องรันด้วย `make test-integration` บนเครื่อง/CI ที่มี Docker เพื่อ verify จริงอีกที
- [x] PDF report: แก้ `internal/application/testresult/generate_report.go` ให้ resolve `Sample.LocationID` เป็น Full Path จริงผ่าน `LocationRepository.FullPath` (fallback `"-"` ถ้าไม่มี location หรือหา full path ไม่เจอ) แทนที่จะโชว์ raw `location_id` — เพิ่ม `ReportData.LocationFullPath`, ผูกผ่าน `internal/adapters/pdf/testresult_report.go` และ `internal/adapters/http/testresult/handler.go`, เพิ่ม `mockLocationRepo` + unit test ใน `internal/application/testresult/generate_report_test.go` (มี location / ไม่มี location) — `go build`/`go vet`/`go test ./...` ผ่านหมด

## Dev Ops

- [x] `git commit` งานทั้งหมด (commit ครบแล้ว, ยังไม่ได้ `git push` ขึ้น origin)
- [x] เชื่อมต่อ Postgres + MinIO cloud instance จริงสำหรับ demo (ตอนนี้ verify ผ่าน container ชั่วคราวที่ลบทิ้งแล้วเท่านั้น) แล้วรัน `make migrate-up` + `go run ./cmd/seed` กับของจริง — เชื่อมกับ instance จริงที่ `192.168.0.207` (Postgres:5432, MinIO:9002) ตาม `.env` ที่มีอยู่แล้ว (แก้ `DATABASE_URL` เพิ่ม `?sslmode=disable` เพราะ server ไม่ได้เปิด SSL), ติดตั้ง `golang-migrate` CLI ผ่าน `brew install golang-migrate`, รัน `make migrate-up` ผ่านครบ 15 migrations, รัน `go run ./cmd/seed` สร้างข้อมูลตัวอย่างสำเร็จ (users/samples/test results/equipment/inventory/documents ผ่าน MinIO/gauges/notifications), บูต `go run ./cmd/api` ยืนยันเชื่อมต่อ DB จริงและ auto-reorder job ทำงานได้ปกติ

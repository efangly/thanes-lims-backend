# Thanes LIMS Backend — งานที่เหลือ (Phase ถัดไป)

## Sample Registry / Equipment / Inventory — requirement ใหม่ (grill เสร็จ 2026-08-27)

Requirement ใหม่ทั้งชุดผ่าน grilling session แล้ว (`/grill-with-docs`) — ทุก decision บันทึกไว้ใน [`CONTEXT.md`](./CONTEXT.md) แล้ว (หัวข้อ Sample Registry, Storage Location (generalized), Vendors, Equipment, Inventory) และมี ADR สองอันสำหรับจุดที่กลับตัวยาก/ผิดจาก convention ทั่วไป: [`docs/adr/0007-shared-location-tree-for-equipment-and-inventory.md`](./docs/adr/0007-shared-location-tree-for-equipment-and-inventory.md) และ [`docs/adr/0008-negative-inventory-lot-quantity-on-forced-over-issue.md`](./docs/adr/0008-negative-inventory-lot-quantity-on-forced-over-issue.md)

แบ่งเป็น 9 phase เรียงตาม dependency — phase หลังพึ่ง phase ก่อนหน้า (Vendor/Location/Custodian เป็น foundation ที่ Sample/Equipment/Inventory ทั้งหมดใช้ร่วมกัน) แต่ละ phase ทำแยก session ได้ ให้ session ใหม่เริ่มจาก checkbox แรกที่ยังไม่ติ๊ก อ่าน CONTEXT.md หัวข้อที่เกี่ยวข้องก่อนเริ่มเสมอ

- [x] **Phase 1 — Vendor module (foundation)** _(เสร็จ 2026-08-27)_: entity ใหม่ `internal/domain/vendor` (Name unique, ContactName, ContactPhone, ContactEmail, Address optional), ports+postgres adapter+migration, application use cases (create/get/list/update — ไม่มี delete ตามที่ไม่เคยคุยกัน ถ้าจำเป็นค่อย grill เพิ่ม), http handler+routes+swagger เก็บไว้เป็น master data page แยก ยังไม่ต้องผูกกับ Equipment/Inventory/PO ใน phase นี้ (รอ phase ที่ใช้จริง)
- [x] **Phase 2 — Location generalization (foundation)** _(เสร็จ 2026-08-27: kind=building→room→zone→cabinet→shelf, barcode LOC-BC-{seq5}, backfill node เก่าทั้งหมด)_: เพิ่ม `Kind` discriminator บน `Location` (`sample_storage` = tree เดิม, `equipment_storage` = tree ใหม่ share กันระหว่าง Equipment/Inventory ตาม ADR 0007) + migration data เดิมทั้งหมด backfill เป็น `sample_storage`; เพิ่ม level types ของ `equipment_storage` (ยังไม่ได้ตกลงชื่อ level ตอน grill — ต้องถามคุณตอนเริ่ม phase นี้ว่าจะใช้ level อะไร เช่น building/room/bench); เพิ่ม `BarcodeCode` (unique, nullable, auto-gen ตอนสร้าง node) บน `Location` เดิมทั้งสอง kind + endpoint lookup-by-barcode
- [x] **Phase 3 — Custodian → User FK (foundation)** _(เสร็จ 2026-08-27)_: `Sample.Custodian` (string) → `Sample.CustodianUserID` (int64, FK → users, NOT NULL) — migration `000028` drop+add column (ยืนยันกับผู้ใช้แล้วว่ายังไม่มี data จริง จึงไม่ backfill); create-sample validate ว่า user id มีจริงผ่าน port ใหม่ `portsample.CustodianDirectory` (postgres/user.Repository.FindByID satisfies); CoC "received" step ใช้ชื่อ custodian user เป็น Who; PDF test-result report resolve custodian name (`ReportData.CustodianName`, GenerateReportUseCase รับ CustodianDirectory เพิ่ม); Oracle POC mirror decouple ออกเป็น `oraclesample.MirrorSample` (POC ADB ไม่มี users table — custodian ยังเป็น free text ที่นั่น); http dto + swagger docs (`custodian_user_id`) + seed (cycle ตาม seeded users). **ยังไม่ได้เพิ่ม field นี้ลง Equipment/Inventory** (entity ยังไม่มี — Phase 5/7)
- [x] **Phase 4 — Sample Registry: Barcode + filter + popup fields** _(เสร็จ 2026-08-27)_: `Sample.BarcodeID *string` (optional, user-supplied ตอน create หรือ auto-gen `SMP-BC-{seq5}` ผ่าน `POST /samples/{id}/barcode` — idempotent ต่อ sample, unique partial index) + `Sample.Description` (migration `000029`); sticker renderer `internal/adapters/pdf/sample_sticker.go` — 4 template: `cap` 9.5×6.4, `stem` 20.5×6.5, `small` 40×20, `medium` 60×30mm; symbology `code128` + `qr` (lib `github.com/boombuler/barcode` + `gofpdf/contrib/barcode`); `GET /samples/{id}/sticker?template=&symbology=` (ถ้าไม่มี BarcodeID → encode Sample ID แทน); filter `GET /samples?barcode_id=` (exact) `&custodian_user_id=` `&location=` (ILIKE ชื่อ leaf Location, join locations); create-sample popup รับ `barcode_id` + `description`; `PATCH /samples/{id}/location` รับ `location_barcode_code` เป็นทางเลือกแทน `location_id` (resolve ผ่าน `AssignLocationUseCase.ExecuteByBarcode` → Phase 2 FindByBarcode). swagger regen แล้ว
- [x] **Phase 5 — Equipment: new fields + documents** _(เสร็จ 2026-08-27)_: เพิ่ม field บน `Equipment` — `SerialNumber` (unique partial index active), `Category` (แยกจาก `TypeCode`), `Manufacturer`, `Model`, `InstallationDate`, `VendorID` (FK → vendors, nullable), `LocationID` (FK → locations, validate Kind = `equipment_storage` ใน use case ตาม ADR 0007) — migration `000030`; ใส่ครบทั้ง create + `PATCH /equipment/{id}` ใหม่ (partial update ผ่าน `UpdateEquipmentUseCase`, ActionEdit) — calibration dates ยังแก้ผ่าน `/calibration` เท่านั้น. Directory ports ใหม่ `portequipment.VendorDirectory` / `LocationDirectory` (postgres vendor/location repo satisfy). **Document ↔ Equipment**: เลือก nullable FK `Document.EquipmentID` (single-owner ไม่ใช่ join table) — migration `000031`; upload รับ `equipment_id` form field (validate ผ่าน `portdocument.EquipmentDirectory`), filter `GET /documents?equipment_id=`, repo `ListByEquipment`; Document `Type` ใหม่ `warranty`. swagger regen แล้ว. seed ยังไม่ผูก vendor/location (directories = nil, validation ข้าม)
- [x] **Phase 6 — Equipment calibration: schedules + results page** _(เสร็จ 2026-08-27)_: `CalibrationSchedule` entity ใหม่ (`internal/domain/equipment/calibration_schedule.go` — EquipmentID, Label, NextDueDate, IntervalMonths optional, `Advance()` helper) + port `portequipment.ScheduleRepository` + postgres adapter (`schedule_repository.go`, table `calibration_schedules`, soft-delete column แต่ Delete เป็น hard delete) + migration `000032`; CRUD ผ่าน `CalibrationScheduleUseCase` (create/list/update/delete) → routes `/equipment/{id}/calibration-schedules[/{scheduleId}]`. Auto-advance: `RecordCalibrationUseCase` รับ ScheduleRepository เพิ่ม — หลัง append event ถ้า `CalibrationType` ตรงกับ `Label` (case-insensitive) และ schedule มี `IntervalMonths` → เลื่อน `NextDueDate`. ขยาย `CalibrationEvent` + `calibration_events` columns (`calibration_type`, `calibrate_value`, `acceptance_value`, `result` — `CalibrationResult` pass/fail/'' + Valid) — migration `000032`. `SearchCalibrationResultsUseCase` + `GET /calibration-results` (cross-equipment, search bar `q`/`equipment_id`/`result`/`from`/`to` ตาม requirement 2.2.1) via `CalibrationRepository.Search` + `FindByID`. Document `Type` ใหม่ `certificate` + nullable FK `Document.CalibrationEventID` — migration `000033`; upload รับ `calibration_event_id` (validate ผ่าน `portdocument.CalibrationEventDirectory` = calibrationRepo), filter `GET /documents?calibration_event_id=`, repo `ListByCalibrationEvent`. Seed: 2 schedule ต่อ equipment (ภายใน 12-เดือน interval + ภายนอก). swagger regen แล้ว. tests: domain `Advance` + auto-advance + schedule CRUD validation
- [x] **Phase 7 — Inventory: new fields** _(เสร็จ 2026-08-27)_: เพิ่มบน `InventoryItem` — `CustodianUserID` (FK → users, NOT NULL, ยืนยันแล้วยังไม่มี data จริง → migration `000034` add column NOT NULL ตรงๆ ไม่ backfill, รูปแบบเดียวกับ Phase 3), `Manufacturer` (free text), `VendorID` (FK → vendors, nullable), `LocationID` (FK → locations, validate Kind = `equipment_storage` ใน use case ตาม ADR 0007). `DefaultVendor` (string เดิม, ใช้กับ auto-reorder) **เก็บไว้ทั้งคู่** คู่กับ `VendorID`. Directory ports ใหม่ `portinventory.CustodianDirectory` / `VendorDirectory` / `LocationDirectory` (`resolve_refs.go` mirror ของ equipment). ใส่ครบทั้ง `CreateItemUseCase` + `PATCH /inventory/{id}` ใหม่ (`UpdateItemUseCase`, partial update, ActionEdit — `Quantity` ยังแก้ผ่าน `/quantity`, `DefaultVendor` ผ่าน `/default-vendor` เท่านั้น). http dto + `ItemResponse` (`custodian_user_id`/`manufacturer`/`vendor_id`/`location_id`) + swagger regen. seed: custodian cycle ตาม seeded users, vendor/location directories = nil (validation ข้าม). tests: create/update validation (custodian required/unknown, non-equipment-storage location, partial update + clear vendor)
- [x] **Phase 8 — InventoryLot + รับของเข้าคลัง** _(เสร็จ 2026-08-27)_: entity ใหม่ `InventoryLot` (`internal/domain/inventory/inventory_lot.go` — ItemID, LotNo, ExpireDate optional, Quantity) + port `portinventory.LotRepository` + postgres adapter (`lot_repository.go`, table `inventory_lots`, `(item_id, lot_no)` unique) + migration `000035` ที่ **drop `inventory_items.quantity`** — Quantity เป็น derived, `postgres/inventory.Repository` sum lot ตอน `FindByID`/`List` (down migration re-add + backfill). `ReceiveStockUseCase` → `POST /inventory/{id}/receive` (`lot_no` required, `expire_date` optional, `quantity` > 0 — lot_no ซ้ำ = บวกเข้า lot เดิม, ไม่งั้นสร้าง `LOT-{seq5}`) + `ListLotsUseCase` → `GET /inventory/{id}/lots`. **ลบทิ้ง**: `PATCH /inventory/{id}/quantity`, `UpdateQuantityUseCase`, `Repository.UpdateQuantity`, field `quantity` ตอน create item. `MarkReceivedUseCase` (PO receipt) เปลี่ยนไปสร้าง `InventoryLot` — รับ `MarkReceivedInput{ID, LotNo, ExpireDate}`, `PATCH /purchase-orders/{id}/receive` ต้องมี body `lot_no`. seed: opening stock เข้าเป็น lot `OPENING`. swagger regen แล้ว. tests: receive (new/topup/validation), mark-received (lot booking/validation)
- [x] **Phase 9 — Stock Issue (เบิกออก)** _(เสร็จ 2026-08-27)_: `IssueStockUseCase` (`internal/application/inventory/issue_stock.go`) → `POST /inventory/{id}/issue` (ActionEdit) — body `{lines:[{lot_id, quantity>0}], force}` multi-line issue ต่อ lot, เลือก lot เจาะจงเอง (ไม่ auto-FEFO). Validate: อย่างน้อย 1 line, lot มีจริง + เป็นของ item นี้, ห้าม lot_id ซ้ำใน lines. ถ้า `quantity` เกินยอดคงเหลือ lot และ **ไม่ได้** ส่ง `force` → ไม่หักอะไรเลย ตอบ `{applied:false, shortfalls:[{lot_id,lot_no,requested,available}], item, lots}` (ยอดคงเหลือจริง — frontend ให้ระบุ lot เพิ่มหรือส่งใหม่พร้อม `force:true`). ถ้า `force:true` → หักทุก line ยอม Quantity ติดลบตาม ADR 0008 (ไม่ reject). ใช้ `LotRepository.FindByID` + `UpdateQuantity` ที่มีอยู่แล้ว — **ไม่มี ledger table / migration ใหม่** (audit middleware บันทึก field-diff ของ item เมื่อ applied). ไม่แตะ seed. swagger regen แล้ว. tests: draw-down, over-issue-no-force, force-negative, multi-line, lot ของ item อื่น, duplicate line, empty lines

**สถานะรวม: Phase 1-9 เสร็จแล้ว (2026-08-27) — requirement ชุด Sample Registry / Equipment / Inventory ครบทุก phase**

---

## Frontend requests (grilling ฝั่ง frontend 2026-08-28)

`/Users/tng-mac-01/Development/web/lab/thanes-lims/docs/backend-requests.md` — สองข้อ read-only ล้วน (เพิ่ม endpoint/field ไม่มี migration ไม่แตะ use case เดิม) แก้ N+1 request ต่อการเปิดหนึ่งหน้า แบ่ง 2 phase อิสระ

- [x] **Phase A — `GET /calibration-schedules` ข้ามเครื่อง** _(เสร็จ 2026-08-28)_: บล็อก frontend phase 6 (ADR-0006 ตาราง 32 เครื่อง = 32 request). เพิ่ม `ScheduleRepository.List(ctx, equipmentIDs []string)` (nil/empty = ทั้งหมด, order `next_due_date`) ใน port + postgres adapter; `CalibrationScheduleUseCase.List`; handler `ListAllCalibrationSchedules` → `GET /calibration-schedules?equipment_id=id1,id2` (permission `equipment:view`, comma-separated filter) reuse `CalibrationScheduleResponse` เดิม; route top-level ข้าง `/calibration-results`. test: list ทั้งหมด + filter ตาม ids. swagger regen
- [x] **Phase B — `earliest_expire_date` + `lot_count` บน `ItemResponse`** _(เสร็จ 2026-08-28)_: บล็อก frontend phase 8 (คอลัมน์ "หมดอายุใกล้สุด" + KPI). เพิ่ม derived field `EarliestExpireDate *time.Time` / `LotCount int` บน `InventoryItem` (overlay เหมือน `Quantity`); `postgres/inventory.Repository.sumByItem` เปลี่ยนคืน `map[string]lotAgg` — query เดิมเพิ่ม `COUNT(*)` + `MIN(expire_date)` (ข้าม NULL ตาม SQL) ไม่มี query ใหม่; overlay ใน `FindByID`/`List`; `ItemResponse` + `toResponse` เพิ่ม `earliest_expire_date` (nullable) + `lot_count`. swagger regen
